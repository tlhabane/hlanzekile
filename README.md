
# Hlanzekile River & Ocean Cleaning Website

**Prerequisites:**  Node.js
---
*Install dependencies:*
`npm install`

### Run Locally

`npm run dev`

### Build & Deploy
1. Compile the website: `npm run build`
2. Zip or compress all files in the build folder and upload these files in the root (`www` or `public_html`) folder of your hosting account